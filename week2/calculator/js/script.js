function initialCalcutor() {
    number = 0;
    resultPlace = document.getElementById("resultPlace");
}

function clickOne() {
    num = 1
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

    function clickTwo() {
        num = 2
        if (resultPlace.value != 0) {
            resultPlace.value = resultPlace.value + num.toString();
        } else {
            resultPlace.value = num;
        }
    }

function clickThree() {
    num = 3
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

function clickFour() {
    num = 4
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

function clickFive() {
    num = 5
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

function clickSix() {
    num = 6
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

function clickSeven() {
    num = 7
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

function clickEight() {
    num = 8
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

function clickNine() {
    num = 9
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}

function clickZero() {
    num = 0
    if (resultPlace.value != 0) {
        resultPlace.value = resultPlace.value + num.toString();
    } else {
        resultPlace.value = num;
    }
}
